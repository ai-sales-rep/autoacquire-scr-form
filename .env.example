# HubSpot private-app token. Scopes: crm.objects.tickets.write,
# crm.objects.contacts.read, crm.objects.contacts.write, crm.schemas.tickets.read
HUBSPOT_TOKEN=

# Slack incoming webhook for the channel that should get SCR alerts
SLACK_WEBHOOK_URL=

# Optional. Defaults shown.
HS_PORTAL_ID=43668701
# Skip the label lookup by pinning ids (find them in HubSpot pipeline settings):
HS_PIPELINE_ID=
HS_STAGE_INTAKE_ID=
